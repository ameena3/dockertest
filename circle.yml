# The deployment section should be excluded until
# it is explicitly included in module 5
#added new comment
#test 5

deployment:
  dockerhub:
    branch: master
    commands:
      - $dockertrigger

test:
  override:    
    - mocha